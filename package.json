{
  "name": "nad-constructora",
  "version": "1.0.0",
  "description": "Sitio web NAD Constructora con panel de administración",
  "main": "server.js",
  "scripts": {
    "harden": "node scripts/patch-server-security.js && node scripts/patch-project-page.js && node scripts/patch-csp-security.js && node scripts/patch-password-policy.js && node scripts/patch-admin-security.js && node scripts/patch-client-security.js",
    "start": "npm run harden && node -r ./security-bootstrap.js server.js",
    "dev": "npm run harden && nodemon server.js",
    "check": "node --check server.js && node --check security-bootstrap.js && node --check lib/security-config.js && node --check lib/sqlite-session-store.js && node --check scripts/patch-server-security.js && node --check scripts/patch-project-page.js && node --check scripts/patch-csp-security.js && node --check scripts/patch-password-policy.js && node --check scripts/patch-admin-security.js && node --check scripts/patch-client-security.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "helmet": "^8.3.0",
    "multer": "^2.0.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "allowScripts": {
    "sqlite3@5.1.7": true
  }
}
